"use client";
import {useEffect,useMemo,useState} from "react"; import {Bell,Check,Clock3,Plus,Trash2,X,Sparkles,AlertTriangle,CalendarDays,CheckCircle2} from "lucide-react";
type D={id:string;title:string;description:string;due:string;priority:string;done:boolean};
const seed:D[]=[{id:"1",title:"Digital Electronics Assignment",description:"Chapter 4 problems",due:new Date(Date.now()+18e6).toISOString(),priority:"High",done:false},{id:"2",title:"SIH Presentation",description:"Finalize technical slides",due:new Date(Date.now()+100e6).toISOString(),priority:"Critical",done:false}];
const left=(s:string)=>{const n=new Date(s).getTime()-Date.now();if(n<=0)return"Overdue";const h=Math.floor(n/36e5),m=Math.floor(n%36e5/6e4);return h>=24?Math.floor(h/24)+"d "+h%24+"h":h?h+"h "+m+"m":m+"m"};
export default function Dashboard(){const[a,setA]=useState<D[]>([]),[modal,setModal]=useState(false),[text,setText]=useState(""),[title,setTitle]=useState(""),[due,setDue]=useState(""),[priority,setPriority]=useState("Medium");useEffect(()=>{const x=localStorage.getItem("dg");setA(x?JSON.parse(x):seed)},[]);useEffect(()=>{if(a.length)localStorage.setItem("dg",JSON.stringify(a))},[a]);const active=useMemo(()=>a.filter(x=>!x.done),[a]);function add(){if(!title||!due)return;setA(v=>[...v,{id:crypto.randomUUID(),title,description:"",due:new Date(due).toISOString(),priority,done:false}]);setTitle("");setDue("");setModal(false)}function extract(){if(!text)return;setTitle(text.replace(/\s+(by|on)\s+.+$/i,""));setDue(new Date(Date.now()+864e5).toISOString().slice(0,16));setModal(true)}return <main className="min-h-screen"><header className="border-b border-zinc-900"><div className="mx-auto flex max-w-7xl items-center justify-between px-5 py-4"><div className="flex items-center gap-3"><div className="grid h-9 w-9 place-items-center rounded-xl bg-lime-300 text-black"><Bell size={18}/></div><b>Deadline Guardian</b></div><button onClick={()=>setModal(true)} className="flex items-center gap-2 rounded-xl bg-lime-300 px-4 py-2 text-sm font-bold text-black"><Plus size={16}/> Add deadline</button></div></header><div className="mx-auto max-w-7xl px-5 py-8"><div className="grid gap-4 md:grid-cols-4">{[[Clock3,"Active",active.length],[AlertTriangle,"Overdue",active.filter(x=>new Date(x.due)<new Date()).length],[CalendarDays,"Today",active.filter(x=>new Date(x.due).toDateString()===new Date().toDateString()).length],[CheckCircle2,"Completed",a.filter(x=>x.done).length]].map(([I,t,n]:any)=><div className="glass rounded-2xl p-5" key={t}><div className="flex justify-between text-zinc-500"><span>{t}</span><I className="text-lime-300" size={18}/></div><div className="mt-3 text-3xl font-black">{n}</div></div>)}</div><div className="glass mt-6 rounded-2xl p-5"><div className="flex items-center gap-2"><Sparkles size={18} className="text-lime-300"/><b>Quick deadline capture</b></div><p className="mt-1 text-sm text-zinc-500">Paste a sentence containing a deadline.</p><div className="mt-4 flex gap-2"><input value={text} onChange={e=>setText(e.target.value)} placeholder="Submit the report by Friday at 5 PM" className="min-w-0 flex-1 rounded-xl border border-zinc-800 bg-zinc-950 px-4 py-3"/><button onClick={extract} className="rounded-xl bg-zinc-100 px-4 font-bold text-black">Extract</button></div></div><section className="mt-8"><h2 className="mb-3 text-sm font-bold uppercase tracking-wider text-zinc-500">Your deadlines</h2><div className="space-y-3">{active.map(x=><article className="glass rounded-2xl p-4" key={x.id}><div className="flex items-center gap-3"><button onClick={()=>setA(v=>v.map(y=>y.id===x.id?{...y,done:true}:y))} className="grid h-9 w-9 place-items-center rounded-lg border border-zinc-700 hover:border-lime-300"><Check size={16}/></button><div className="min-w-0 flex-1"><b>{x.title}</b><div className="mt-1 flex gap-2 text-sm text-zinc-500"><Clock3 size={14}/> {left(x.due)} remaining · {new Date(x.due).toLocaleString()}</div></div><span className="text-xs text-zinc-500">{x.priority}</span><button onClick={()=>setA(v=>v.filter(y=>y.id!==x.id))}><Trash2 size={16} className="text-zinc-600 hover:text-rose-400"/></button></div></article>)}</div></section></div>{modal&&<div className="fixed inset-0 grid place-items-center bg-black/70 p-5"><div className="glass w-full max-w-lg rounded-2xl p-6"><div className="flex justify-between"><h2 className="text-xl font-bold">Add deadline</h2><button onClick={()=>setModal(false)}><X/></button></div><input value={title} onChange={e=>setTitle(e.target.value)} placeholder="Task name" className="mt-5 w-full rounded-xl border border-zinc-800 bg-zinc-950 p-3"/><input type="datetime-local" value={due} onChange={e=>setDue(e.target.value)} className="mt-3 w-full rounded-xl border border-zinc-800 bg-zinc-950 p-3"/><select value={priority} onChange={e=>setPriority(e.target.value)} className="mt-3 w-full rounded-xl border border-zinc-800 bg-zinc-950 p-3"><option>Low</option><option>Medium</option><option>High</option><option>Critical</option></select><button onClick={add} className="mt-4 w-full rounded-xl bg-lime-300 py-3 font-bold text-black">Create deadline</button></div></div>}</main>}